#include <stdio.h>
#include <string.h>
#define _CRT_SECURE_NO_WARNINGS
int main()
{
    int n;
    scanf("%d", &n);
    int x[100000], y[100000];

    for (int i = 0; i < n; i++)
    {
        scanf("%d %d", &x[i], &y[i]);
    }

    for (int i = 0; i < n; i++)
    {
        
    }
    

    return 0;
}